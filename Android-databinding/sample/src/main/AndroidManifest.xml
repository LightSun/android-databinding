<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.heaven7.databinding.demo" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".TestApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".samples.BaseBehaviourActivity"
            android:label="BaseBehaviourActivity"
            />
        <activity android:name=".samples.ListViewBindAdapterTest"
            android:label="ListViewBindAdapterTest"
            />
        <activity android:name=".samples.RecycleViewBindAdapterTest"
            android:label="RecycleViewBindAdapterTest"
            />
        <activity android:name=".samples.TextChangeTest"
            android:label="TextChangeTest"
            />
    </application>

</manifest>
